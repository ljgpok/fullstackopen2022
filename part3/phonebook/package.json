{
  "name": "phonebook",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "nodemon index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:ui": "rm -rf build && cd ../../part2/phonebook && npm run build && cp -r build ../../part3/phonebook",
    "deploy": "cd ../../ && git subtree push --prefix part3/phonebook heroku main",
    "deploy:full": "npm run build:ui && cd ../../ && git add . && git commit -m uibuild && npm run deploy",    
    "logs:prod": "cd ../../ && heroku logs --tail"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "nodemon": "^2.0.19"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.1",
    "morgan": "^1.10.0"
  }
}
